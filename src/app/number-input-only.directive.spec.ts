import { NumberInputOnlyDirective } from './number-input-only.directive';

describe('NumberInputOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberInputOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
